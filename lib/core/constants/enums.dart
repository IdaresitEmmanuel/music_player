enum ThemeSetting {
  key,
  light,
  dark,
}

enum PlaylistType {
  artist,
  album,
  folder,
// userPlaylist
}

enum SearchCategory {
  key,
  songs,
  aritst,
  album,
  folder,
}

enum KeyManager { newPlaylistKey }

enum SongWidgetType {
  songs,
  search,
  playlist,
}
